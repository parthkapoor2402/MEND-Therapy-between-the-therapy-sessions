{
  "installCommand": "cd mend-app && npm install",
  "buildCommand": "cd mend-app && npm run build",
  "outputDirectory": "mend-app/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
